пусть в каждой папке должен быть набор файлов, описывающих блок:
1) унаследованный от base_scene_item класс, описывающий как блок выглядит на сцене
2) унаследованный от block_gui класс, описывающий графический интерфейс блока через который мы будем
устанавливать параметры блока
3) [пока не нужен] файл реализации блока, который нужен для валидации параметров и потом для кодогенерации